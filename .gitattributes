# Normalize line endings to LF in the repo AND the working tree.
# This project is built on Linux (Docker clean-room) and cross-compiled to
# bare-metal, so LF everywhere avoids spurious CRLF diffs and tool confusion
# on Windows checkouts.
* text=auto eol=lf
